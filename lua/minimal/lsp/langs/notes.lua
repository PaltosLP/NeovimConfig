
To install gopls:
	- Install the binary via lsp-installer
	- sudo mv gopls /bin


Pyright:
    - npm i -g pyright

vim-language-server:
	- npm install -g vim-language-server

lua-language-server:
	- sudo apt install unzip
	- install homebrew
	- brew install lua-language-server

clangd:
	- sudo apt install clangd

